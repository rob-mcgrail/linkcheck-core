require 'rubygems'
require './lib/tki-linkcheck'

LinkCache.force_flush

sites = Sites.partition_by_age[1]

sites.each do |site|
  begin
    Crawler.new(site).crawl
    GC.start # haha nice try
  rescue Exception => e
    puts e
  end
end
